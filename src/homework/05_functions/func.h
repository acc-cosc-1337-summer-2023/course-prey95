//add function(s) prototype here
#include <iostream>
#include <vector>

using std::cout;
using std::cin;
using std::vector;

void show_menu();
void UI();
void run_menu(int choice);
int get_vector_max_value(vector<int> x);
vector<int> squareVec(vector<int> x);